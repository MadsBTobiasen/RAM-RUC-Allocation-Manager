@page
@using RAM___RUC_Allocation_Manager.Models
@model RAM___RUC_Allocation_Manager.Pages.AddUser.AddUserModel
@{
}

@section Scripts
{
    <script>
        $(document).ready(function () {
            $("#myModal").modal({ backdrop: "static" });
                $("#myModal").modal("show");
            });
    </script>
}

<div class="row">

    <div class="col-1"></div>
    <div class="col-10">
        <div class="container" style="display: flex; justify-content: center;">
            <img src="~/assets/ram-full-icon-bg.png" class="ram-icon-bg">
        </div>
    </div>
    <div class="col-1"></div>

</div>

<form method="post">
    <div class="container">
        <div class="modal" id="myModal">
            <div class="modal-dialog">
                <div class="modal-content">

                    <div class="modal-header">
                        <h2 class="modal-title">Opret Leder</h2>
                        <span><a asp-page="../LeaderLandingPage/LeaderLandingPage"><i class="fa-solid fa-lg fa-xmark"></i></a></span>
                    </div>

                    <div class="modal-body">

                        <div class="form-group">
                            <label asp-for="@Model.User.Type" class="control-label"></label>
                            <select class="form-control" asp-for="@Model.User.Type"
                                    asp-items="Html.GetEnumSelectList<User.UserType>()">
                            </select>
                        </div>

                        <div class="form-group">
                            <label asp-for="@Model.User.Name" class="control-label"></label>
                            <input asp-for="@Model.User.Name" class="form-control" placeholder="Indtast navn på leder.." />
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.User.Password" class="control-label"></label>
                            <input asp-for="@Model.User.Password" class="form-control" placeholder="Indtast password for ny leder.." />
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.User.Username" class="control-label"></label>
                            <input asp-for="@Model.User.Username" class="form-control" placeholder="Indtast brugernavn for ny leder.." />
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.User.Email" class="control-label"></label>
                            <input asp-for="@Model.User.Email" class="form-control" placeholder="Indtast email for ny leder.." />
                        </div>

                    </div>

                    <div class="modal-footer">

                        <div class="row w-100">

                            <div class="col-6">
                                <a type="button" class="w-100 btn btn-danger" asp-page="../LeaderLandingPage/LeaderLandingPage">Annullér</a>
                            </div>
                            <div class="col-6">
                                <input type="submit" value="Opret Leder" class="w-100 btn btn-primary">
                            </div>

                        </div>

                    </div>

                </div>

            </div>
        </div>
    </div>
</form>